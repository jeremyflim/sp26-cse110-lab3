# Issue #7 Resolution

CSS validation artifact is stored in:
- `screenshots/w3validator.png`

Notes:
- The validator may report warnings for newer selectors such as `:has()` and nested CSS syntax.
- Those selectors were intentionally included to satisfy the Lab 3 checklist.
